import 'package:flutter/material.dart';

class MyOpinions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("MyOpinions"),
      ),
    );
  }
}