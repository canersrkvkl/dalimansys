import 'package:flutter/material.dart';

class NewPurches extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("New Purches"),
      ),
    );
  }
}